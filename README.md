# QueueSmart

## Setup & run

### 1. Clone the repo

```bash
git clone <repo-url>
cd QueueSmart
```

### 2. Install dependencies & run dev server

From repo root:
```bash
pnpm run install:frontend
pnpm dev
```

Or from the frontend folder:
```bash
cd frontend
pnpm install
pnpm dev
```